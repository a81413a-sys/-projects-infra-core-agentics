# Command: /onboard
# Description: Initialize a new client project and gather requirements.

1. Create a directory: `shared-memory/client/current/{client_name}`.
2. Generate `requirements.md` based on client input.
3. Notify the COO to begin the dispatch phase.
